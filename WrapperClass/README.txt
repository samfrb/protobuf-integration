Wrapper class
=============

The ./Proto folder contains the proto definitions and the 
generated classes.

The idea behind the "Wrapper class" pattern is that the data 
deserialized from the byte array to a protobuf class is used
to build (through constructor) the Transaction.